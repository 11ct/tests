list = [7,9,4,6,3,7,2,1,8]

for i in range(0,len(list)):
    for j in range(0,len(list)-i-1):
        if list[j] > list[j+1]:
            temp = list[j]
            list[j] = list[j+1]
            list[j+1] = temp

for x in range(0, len(list)):
    print(list[x])