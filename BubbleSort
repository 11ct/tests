list = [1,2,3,4,5,6,7,8,9]


def function(list):
    swapped = False
    for i in range(0,len(list)):
        for j in range(0,len(list)-i-1):
            if list[j] > list[j+1]:
                temp = list[j]
                list[j] = list[j+1]
                list[j+1] = temp
                swapped = True
            if swapped == False:
                print("asdasdoaiwjdoiwjoasd")
                break
    
function(list)
for x in range(0, len(list)):
    print(list[x])